export interface IUserDetails{
     usId:number;
     usUsername:string;
     usPassword:string; 
     usFirstName:string; 
     usLastName:string; 
     usContactNumber:number; 
     usRegDateTime:Date; 
     usRegCode:string; 
     usForceResetPassword:string; 
     usActive:string; 
}